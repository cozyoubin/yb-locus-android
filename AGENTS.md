# YB LOCUS 개발 규칙

이 규칙은 프로젝트 전체에 적용한다. 기준 소스는 Android v2.8이다.
현재 작업 범위는 구조 분석과 문서화이며, PHASE 1 구현은 별도 후속 작업이다.

## 고정 규칙

1. 기본 매물유형은 상가 + 사무실.
2. 기본 거래유형은 매매 + 월세.
3. 기본 면적단위는 평.
4. 앱 시작 중 아파트/재건축/매매·전세 화면을 사용자에게 노출하지 않는다.
5. 지도 이동과 투자분석을 분리한다.
6. 지도 이동만으로 배후세대/투자분석을 반복 실행하지 않는다.
7. 분석은 사용자의 명시적 액션으로 실행한다.
8. 배후세대 조회는 동시에 1개 요청만 활성화한다.
9. 동일 물건 분석결과는 캐시 가능한 구조로 설계한다.
10. 상세화면에서 Android 뒤로가기는 앱 종료가 아니라 직전 YB LOCUS 상태로 복귀한다.
11. 숫자 계산은 결정론적 코드가 담당하고 AI는 설명만 담당한다.
12. 기존 정상 기능을 수정할 때 회귀 영향을 먼저 확인한다.

## 작업 방법

- 변경 전 `docs/ARCHITECTURE.md`의 기능 위치와 결합부, `docs/PHASE1_PLAN.md`의 검증 기준을 읽는다.
- 규칙은 목표 계약이다. 현재 코드가 이미 모두 준수한다고 가정하지 않는다.
- 기본값은 초기 진입 정책이다. 초기화 완료 후 사용자의 유형·거래·단위 선택을 반복해서 덮어쓰지 않는다.
- 화면 열기/복귀와 분석 실행을 구분한다. 사용자 분석 액션으로 얻은 입력 스냅샷을 사용하고, 렌더링에서 네트워크 요청을 만들지 않는다.
- 배후세대는 상위 조회 작업 하나만 활성화하고, 하위 HTTP 요청도 초기 구현에서는 직렬화한다. 취소 완료 전에 대체 작업을 시작하지 않는다.
- 숫자 공식·반올림·TOP3 순위 기준을 구조 변경과 함께 몰래 바꾸지 않는다. 동일 입력의 기존 결과를 회귀 기준으로 삼는다.
- Java ↔ JavaScript 브리지 이름, 인수, 주입 순서 변경 시 양쪽 호출부와 뒤로가기 경로를 함께 확인한다.
- 외부 사이트의 DOM/API에 의존하는 동작은 정적 분석과 실제 기기 검증을 구분하여 보고한다.
- API 키, 서명키, 비밀번호, 쿠키, 인증 헤더, 사용자 저장소 덤프를 소스·문서·fixture·로그에 넣지 않는다. `assets/config.json`은 공개 설정만 담는다.
- `.gitignore`는 이미 추적된 비밀을 제거하지 않는다. 커밋 전 staged diff와 비밀정보 검사를 수행하고, 실제 비밀 유출 시 키 교체와 이력 정리를 별도로 처리한다.
- 테스트 및 빌드를 실제로 실행하지 못했다면 그 이유를 명시한다. 현재 Gradle Wrapper와 자동 테스트/CI는 없다.
